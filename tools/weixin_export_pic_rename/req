帮我实现一个py脚本，用于照片重命名
微信导出的照片，格式为 mmexport1643713016351.jpg
将日期，时间转换出来
重命名为类似以下格式
WX_20220201_163221.jpg
WX_ 是固定，后面接日期，后面接时间

脚本遍历处理文件夹的所有 mmexport 开头的图片，复制并重命名，用一个参数控制是否保留原始文件


补充一点，例如 wx_camera_1660043668500.jpg 也是微信导出的照片，也需要执行以上处理
